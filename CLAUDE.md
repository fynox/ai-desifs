# AI-dhésif

Avant de lire des fichiers, consulte d'abord `graphify-out/graph.json` pour comprendre la structure et ne lire que le strict nécessaire (économie de tokens). Après de gros changements de code, mets la carte à jour avec `graphify update .`.
